#!/bin/sh

# TODO add are you sure?

export AWS_DEFAULT_REGION="{{ aws_bootstrap_aws_region }}"

terraform remote config -backend=s3 -backend-config="bucket={{ aws_bootstrap_state_bucket }}" -backend-config="key={{ aws_bootstrap_state_file }}" -disable

terraform destroy

